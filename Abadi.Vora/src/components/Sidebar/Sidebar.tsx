/**
 * Canonical re-export — implementation lives in `@/components/layout/Sidebar`.
 * AppShell and all consumers must use the layout version (single VoraLogo link).
 */
export { Sidebar } from "@/components/layout/Sidebar";
